{
  "name": "@ielgnaw/utils",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "prepack": "yarn build",
    "1build": "yarn clean && tsc --build && cp \"./package.json\" ./dist/",
    "test": "echo 'no test'",
    "lint": "eslint \"./src/**/*.{ts,tsx}\" --max-warnings=0",
    "build": "echo 333"
  },
  "peerDependencies": {
    "vue": "^3.2.0"
  },
  "dependencies": {
    "@popperjs/core": "~2.11.5",
    "vue-types": "~4.1.1"
  }
}
